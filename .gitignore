# package directories
node_modules
jspm_packages

# Serverless directories
.serverless
# Environment variables (never commit real credentials)
.env
.env.uat
.env.local
.env.production

# Maintenance / one-off scripts (contain hardcoded credentials)
scripts/

# Logs
*.log
npm-debug.log*
